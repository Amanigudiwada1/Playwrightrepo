# Taste (Continuously Learned by [CommandCode][cmd])
- Expects the agent to handle git operations (commit and push) so that local changes show up in the remote repo — e.g., when the user can't see an updated file on GitHub, the fix is to commit and push. Standard workflow: stage all changes (`git add -A`), commit with a descriptive message, and push to the remote. Confidence: 0.97
- Keeps study/learning notes (e.g., IQ_notes) as markdown files that mix explanatory text with code — headings, tables, and fenced code blocks in one `.md` file, so notes are readable and the code is shown as runnable examples; explicitly directs that new notes be saved as `.md` files inside the `IQ_Notes` folder. Confidence: 0.9
- Wants study/learning notes (IQ_Notes) to follow a specific visual style: a breakdown table, an example walkthrough using a simple code/file example, a pipeline diagram, and a TL;DR summary. Confidence: 0.9
- Communicates tersely and action-first (e.g., "go go go") — expects the agent to infer the task from the open file and repo state and proceed autonomously without asking clarifying questions or lengthy preamble. Confidence: 0.7
- Asks conceptual "explain me why" questions about language behavior while working through exercises (e.g., why `push()` logs `4`); expects a concise explanation of the underlying mechanism (return values vs. side effects) rather than just a code fix. Confidence: 0.75
- Organizes learning exercises in sequentially numbered files grouped by topic folder (e.g., `10_Arrays\69-Array_Adding_remove.js`), i.e., a structured, numbered curriculum layout. Confidence: 0.55
